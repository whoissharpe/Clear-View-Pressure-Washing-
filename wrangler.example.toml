name = "clear-view-pressure-washing"
compatibility_date = "2026-08-01"
pages_build_output_dir = "dist"

# Lead storage. database_id is filled in by:
#   npx wrangler d1 create clearview-leads
[[d1_databases]]
binding = "DB"
database_name = "clearview-leads"
database_id = "LOCAL-ONLY-REPLACE-ME"
