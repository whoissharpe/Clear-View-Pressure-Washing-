node_modules/
dist/

# Working artefacts from the design pass - not part of the deliverable.
# The optimised renditions in assets/img and assets/video ARE committed;
# these are just the oversized masters and verification renders.
assets/design/check-*
assets/design/comp-*
assets/design/raw-*.png

# Lighthouse report artefacts
lh*.json

# Local Pages/D1 state, and the local wrangler config (it carries a real
# database_id). Copy wrangler.example.toml to wrangler.toml to run locally.
.wrangler/
wrangler.toml
